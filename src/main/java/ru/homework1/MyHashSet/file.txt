1. сгенерить ID
2. проверить ID на null
  - если нал то перезаписываем
  - 3. esle {
            взять объект по id
            сравнить хэш исходного ключа с тем который там лежит
            id(хэши одинаковые) перезаписываем весь объект
            else нужно проверить некстколлижн
                    если оно пустое то пишем иначе вызываем метод обработки коллизий
  }